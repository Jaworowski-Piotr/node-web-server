<!DOCTYPE html>

<html>
    {{> header}}
    <body>
        <h1>Some things new.</h1>
        <h1>{{homeTitle}}</h1>
        <h2>{{getCurrentYear}}</h2>
        <h2>{{screamIT "welcome"}}</h2>
    {{> footer}}
    </body>
</html>